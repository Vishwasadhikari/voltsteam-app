# VoltStream Backend

FastAPI service for the VoltStream prosumer energy dashboard.

## Run

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

API docs: http://localhost:8000/docs
