# VoltStream Frontend

React + Vite dashboard for VoltStream.

## Run

```bash
npm install
npm run dev
```

The frontend expects the backend at `http://localhost:8000`. To use a different API URL, create `.env` with:

```bash
VITE_API_BASE_URL=http://localhost:8000
```
