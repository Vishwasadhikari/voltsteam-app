import React from 'react'
import ReactDOM from 'react-dom/client'
import VoltStream from './VoltStream.jsx'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VoltStream />
  </React.StrictMode>
)
